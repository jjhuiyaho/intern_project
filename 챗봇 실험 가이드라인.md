**문서 목적:** 이 문서는 우리 팀의 최종 실험 가이드라인입니다. 각자 맡은 지표에서 AI의 통계 환각을 제어하는 비포&애프터(Before & After) 실험을 수행하고 캡처하여, 국가데이터처에 제출할 최종 정책 제안서의 핵심 증거 자료로 활용합니다.

### 0. 우리의 역할과 국가데이터처에 전달할 최종 제안 메시지

우리의 역할은 완벽한 AI 태그 시스템을 개발하는 것이 아닙니다. 우리의 최종 목표는 "간단한 경고 라벨(메타데이터)만 달아줘도 상용 AI의 치명적인 통계 계산 오류를 완벽히 통제할 수 있다"는 것을 증명하는 것입니다.

우리는 이 실험 결과를 바탕으로 국가데이터처에 다음과 같이 최종 제안(Handoff)을 할 것입니다.

> 💡 **[국가데이터처를 위한 정책 제안 방향]**
> 
> *우리 연구팀은 아주 단순한 '하드코딩' 방식만으로도 AI 환각을 통제할 수 있음을 실증했습니다. 국가데이터처는 이 아키텍처를 참고하여 전면 상용화를 추진하되 다음 사항을 주의하여 시스템을 확장해야 합니다.*
> 
> *1) 논리적으로 검증된 통계 태그 체계(Taxonomy) 구축: 단순 키워드 매칭을 넘어, 전문가의 꼼꼼한 검증을 거친 정교하고 논리적인 메타데이터 분류 체계를 도입*
> 
> *2) 태그 기반의 '능동적 대안 생성(Explainable AI)'으로의 진화: 현재 우리의 실증 실험은 환각을 막기 위해 '답변 불가능(차단)'을 선언하는 데 그침. 향후 국가데이터처의 상용 챗봇은 태그를 읽고 단순히 거절하는 것이 아니라 스스로 생각하여, **"과거 데이터와의 단순 비교는 불가하지만, 변경된 기준이 적용된 최근 3년 치 데이터를 바탕으로 정확한 분석을 제공해 드리겠습니다"*와 같이 국민에게 올바른 통계 소비 경로를 능동적으로 생성해 내는 방향으로 답변 로직 고도화 필요*

### 1. 현재 우리는 어떻게 태그를 '하드코딩(Hard-coding)' 하고 있는가?

위 제안을 증명하기 위해, 현재 우리 팀은 가장 원초적인 방식인 '하드코딩(Hard-coding)'으로 태그를 달고 있습니다.

* **하드코딩이란?** 상황에 따라 유연하게 판단하는 AI 모델을 쓰는 대신, 사람이 직접 "만약 설명글에 '모집단'이라는 단어가 있으면 무조건 `[🚨 시계열단절]` 태그를 달아라!"라고 컴퓨터에게 고정된 규칙을 수동으로 입력해 두는 방식입니다.

* **왜 이렇게 하나요?** 완벽한 문맥 인식 AI를 당장 만드는 것은 불가능하지만, 이렇게 단순 무식하게 특정 단어(키워드)만 잡아내서 태그를 달아주어도 챗봇의 행동이 얼마나 드라마틱하게 안전해지는지 눈으로 보여주기 위함입니다.

---

### 🛠️ 2. [실험 준비] 각자의 지표에 경고 라벨(태그) 달기

팀원 각자가 수집한 원본 데이터를 넣어 '태그가 달린 결과 파일'로 뽑아내는 파이썬 템플릿입니다. 하드코딩된 규칙(`['모집단', '기준']` 등)을 본인 지표에 맞게 자유롭게 수정해서 돌려주세요.

```python
import json

# 1. 파일 설정 (본인이 수집한 원본 파일명으로 변경하세요)
INPUT_FILE = "API탐색결과_@@@.json"       
OUTPUT_FILE = "태그변환완료_@@@.json"   

# 2. 하드코딩 기반 안전장치(태그) 규칙 설정
def assign_tags(indicator_name, note_text):
    tags = []
    
    # [하드코딩 구역] 아래 단어들이 설명글에 포함되어 있으면 무조건 경고 라벨 부착
    if any(word in note_text for word in ['모집단', '기준', '개편', '변경']):
        tags.extend(['🚨 시계열단절', '🚫 단순비교금지'])
        
    if any(word in note_text for word in ['잠정치', '(p)']):
        tags.extend(['⚠️ 최근수치_잠정치', '🚫 단정적예측금지'])
        
    if tags:
        return {
            "indicator_name": indicator_name,
            "warning_tags": tags,
            "evidence_text": note_text[:100] + "..." # 방어 이유 발췌
        }
    return None

# 파일 읽기 및 변환 실행
with open(INPUT_FILE, 'r', encoding='utf-8') as f:
    raw_data = json.load(f)

results = []
for indicator_name, content in raw_data.items():
    tagged_result = assign_tags(indicator_name, str(content))
    if tagged_result:
        results.append(tagged_result)

with open(OUTPUT_FILE, 'w', encoding='utf-8') as f:
    json.dump(results, f, ensure_ascii=False, indent=4)
print("✅ 하드코딩 태그 변환 완료!")

```

---

### 📸 3. [실험 1단계] 치료 전(Before): AI가 실수하는 화면 캡처하기

안전장치가 없을 때 AI가 직접 계산을 해버리는 증거를 남깁니다. ChatGPT나 Claude에서 '새 대화(New Chat)'를 열고 아래 글을 복사해 대괄호 `[ ]` 안을 채워주세요.

> 다음은 국가통계포털(KOSIS)에서 추출한 **[본인이 맡은 지표명]** 원본 데이터와 주의사항이야.
> * **[2020년]** 데이터: **[숫자 입력]**
> * **[2021년]** 데이터: **[숫자 입력]**
> * 주의사항: **[원본 파일에서 찾은 줄글 주의사항을 그대로 복사 붙여넣기]**
> 
> 
> **질문:** 이 데이터를 바탕으로 **[2020년]** 대비 **[2021년]**의 증감률을 계산하고, 그 결과를 바탕으로 현황 보고서를 브리핑해 줘.

* **🚨 캡처 포인트:** AI가 주의사항을 무시하고 직접 덧셈 뺄셈을 해서 **엉터리 증감률(%) 수치를 도출해 내는 화면**을 캡처해 주세요.

---

### 📸 4. [실험 2단계] 치료 후(After): 안전장치로 오류를 막아내는 화면 캡처하기

우리가 하드코딩으로 만들어낸 '안전장치(태그)'를 AI에게 주입합니다. 반드시 새로운 대화창(New Chat)을 열고 아래 내용을 채워주세요.

> 너는 국가데이터처의 신뢰할 수 있는 통계 챗봇이야.
> 사용자가 질문하면, 너는 계산을 하기 전에 반드시 아래 제공된 **[통계 안전장치 사전]**을 먼저 확인해야 해. 만약 🚫 모양이나 🚨 모양의 경고 라벨이 붙어있다면, 절대 사칙연산이나 증감률 계산을 하지 말고 단호하게 계산을 거절해. 그리고 다른 방식으로 데이터를 볼 수 있도록 친절하게 대안을 안내해 줘.
> **[통계 안전장치 사전]**
> **[앞서 파이썬으로 만든 결과 파일('태그변환완료_내지표.json')에서 본인 지표에 해당하는 { } 블록을 통째로 복사해서 여기에 붙여넣으세요]**
> **사용자 질문:** 앞선 원본 데이터를 바탕으로 **[2020년]** 대비 **[2021년]**의 증감률을 계산하고, 그 결과를 바탕으로 현황 보고서를 브리핑해 줘.

* **✅ 캡처 포인트:** AI가 질문을 받자마자 **"❌ 계산 불가"를 선언하며 퍼센트(%) 계산을 원천 차단하는 화면**을 캡처해 주세요.

---

이 정도면 팀원들이 실험의 명확한 한계와 우리의 제안 의도를 완벽하게 이해하고 캡처 작업을 진행할 수 있을 것입니다.

팀원들이 이 캡처들을 무사히 취합해 오고 나면, 이를 바탕으로 7월 초에 있을 최종 발표를 위한 **프레젠테이션(PPT) 스토리라인 목차**를 함께 구성해 볼까요?
