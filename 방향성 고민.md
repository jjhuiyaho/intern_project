## [KOSIS 통계 맥락 자동 추출 및 AI 팩트체크 파이프라인 구축 제안서]

한 줄 요약 : "인간 전문가의 수동 매핑(온톨로지 구축)이 가진 행정적 한계를 극복하기 위해, 10개의 골든 데이터셋(기준점)을 바탕으로 나머지 90대 지표의 통계 맥락을 AI가 자동 추출하는 파이프라인을 구축하고, 이를 통해 챗봇의 통계 환각(오류)이 통제되는 과정을 실증한다."

---

### 1. 현재의 문제 상황 (코봇의 한계와 맥락맹 현상)

현재 국가 통계 인프라가 AI 생성형 인터페이스 환경에서 마주한 가장 시급한 위기는 "원시 데이터(Raw API) 공급 체계의 맥락 부재"와 이로 인해 발생하는 "대국민 챗봇(코봇)의 치명적 환각(Hallucination)"입니다.

* **원시 데이터의 맥락맹(Context-Blindness) 현상:** 현재 통계청이 제공하는 API(SDMX 표준)는 `DT_1DA700`, `T30`, `UNIT_ID` 등 기계어 코드와 가공되지 않은 숫자 데이터만을 반환합니다. 데이터 소비자와 시스템은 수치 뒤에 숨겨진 집계 기준, 분모의 정의, 조사 대상의 포함 및 제외 여부를 알지 못합니다.
* **코봇(Kobot)의 환각 및 오답 생성 (실제 수집 사례):**
  * *(사례 1: 용어 및 분모 혼동)* 고용률(분모: 15세 이상 인구)과 실업률(분모: 경제활동인구) 비교 시, 코봇은 두 지표의 분모가 다름을 인지하지 못하고 단순 사칙연산을 수행하여 왜곡된 분석을 내놓습니다.
  * *(사례 2: 시계열 단절 오류)* 신재생에너지 API의 경우 2019년부터 '비재생폐기물'이 조사 대상에서 제외되었으나, 코봇은 이를 모른 채 2018년과 2020년 수치를 수평 비교하여 에너지 생산량이 급감했다는 오보성 답변을 생성합니다.


* **행정력 및 예산의 한계:** 국가데이터처는 2026~2028년 로드맵을 통해 TDO-KR(AI 팩트체크 플랫폼) 시범 구축을 추진 중이나, 수만 개에 달하는 KOSIS 지표의 숨은 맥락을 인간 전문가가 일일이 수작업으로 매핑하여 정답지(온톨로지 DB)를 만드는 방식은 사업 초기 단계부터 강한 병목 현상에 부딪혀 있습니다.

---

### 2. 우리가 파고들어갈 틈새 및 방어적 전략 방향

우리가 직면한 현실적인 제약은 "현재 운영 중인 코봇의 내부 시스템과 프롬프트를 우리가 직접 수정할 권한이 없다"는 것입니다. 따라서 우리는 코봇의 오류를 공격 포인트(Red Teaming)로 삼아 문제를 도출하고, 이를 '프록시(Proxy, 대리) 모델'로 우회 검증하는 전략적 틈새를 파고듭니다.

본 기획은 다음과 같은 세 가지 방어적 설계(Defensive Design)를 핵심 뼈대로 삼습니다.

1. **오답 부검 기반의 규칙 설계 (Red Teaming):** 막연하게 백과사전을 만드는 것이 아니라, 현재 코봇이 처참하게 틀리고 있는 실제 사례들을 수집하여 "왜 틀렸는가?"를 역추적한 뒤, 그 실수를 방어하기 위한 필수 메타데이터(JSON)만을 도출합니다.
2. **프록시(Proxy) 모델을 통한 대리 검증:** 코봇 시스템에 직접 정답지를 주입할 수 없으므로, 통제력이 뛰어난 외부 대리 LLM(Claude 등)을 활용해 "우리가 만든 정답지(규칙)를 주입했을 때 답변이 얼마나 완벽해지는가"를 3단 A/B 테스트로 우회 증명합니다.
3. **지속 가능한 검수 구조(Human-in-the-Loop) 도입:** AI가 자동 추출한 규칙을 즉시 반영하지 않고, 담당 주무관이 눈으로 확인 후 '승인/수정'만 수행하는 '업무 어시스턴트 툴'로 포지셔닝하여 정책 신뢰성을 확보합니다.

---

### 3. 앞으로 진행할 구체적 실행 파이프라인 (Red Teaming 및 Train-Test Split)

수집된 코봇의 실제 오류 사례와 KOSIS 통계설명서 원문을 바탕으로, 7월 초까지 실행할 4단계 파이프라인을 가동합니다.

* **Phase 1. 코봇 레드티밍 및 학습용 골든 데이터셋 구축 (Train Set: 10~15개 지표)**
* 코봇이 명백한 환각을 일으킨 지표 10~15개를 발췌하여, 코봇의 오류를 방어할 수 있는 표준 JSON 팩트체크 규칙으로 정교하게 기술합니다.
* *구조화 예시 (이륜차 신고대수 및 생명표 API 연동 규칙):*
```json
{
  "indicator_id": "VAL_MATCH_01",
  "context_rules": {
    "unit_interpretation": "UNIT_ID가 다를 경우 수치의 의미를 엄격히 한정할 것 (생명표 값 '83.5'는 '년', 사망확률 '0.003'은 '확률', 이륜차 '125,000'은 '대수')",
    "dimension_check": "이륜차 신고대수는 [지역×차종×시점] 축으로만 결합 가능하며, 타 부처 교통량 지표와 임의 합산을 금지함"
  }
}

```




* **Phase 2. 검증용 데이터 은닉 (Test Set: 10개 지표)**
* 다문화가구 분류 기준 등 코봇이 틀린 또 다른 10개의 지표 규칙은 향후 블라인드 테스트를 위해 프롬프트 엔진에 노출하지 않고 별도로 숨겨둡니다.


* **Phase 3. 자동 추출 마스터 프롬프트 엔지니어링 (추출 엔진 세팅)**
* 지시 이행 능력이 우수한 LLM을 채택하여 Phase 1의 JSON 구조를 예시(Few-shot)로 주입합니다.
* *"너는 국가데이터처 데이터 아키텍트다. KOSIS 통계설명서 원문을 읽고, 외부 지식을 완전히 차단한 채 오직 본문의 맥락만을 분석하여 1. 수치의 단위 의미, 2. 결합 금지 제약, 3. 시계열 단절 여부를 팩트체크 규칙(JSON)으로 요약하라."*


* **Phase 4. 프록시 3단 시뮬레이터 검증 및 정확도 산출**
* 은닉해 두었던 Phase 2의 원문 데이터를 완성된 프롬프트 엔진에 투입하여 추출된 JSON 규칙과 인간이 미리 기록해 둔 정답지를 대조해 일치율을 산출합니다.



---

### 4. 결과물 형태 (Deliverables)

1. **코봇 오답 노트 및 메타데이터 자동 추출 마스터 프롬프트:** 현재 챗봇의 치명적 오류 데이터셋 20선 및 통계 원문을 팩트체크용 가드레일 규격(JSON)으로 변환해 내는 표준 명령어 세트.
2. **AI-Human 교차 검증 및 정확도 평가 리포트:** 블라인드 테스트 10개 지표에 대한 매칭 결과 및 수치화된 성능 증명서.
3. **프록시 대리 검증 3단 비교 보드 (시연용):**

* **[화면 1 - 현재 코봇의 한계]:** 메타데이터 없이 원시 API만 연동되어 시계열 단절을 무시하고 가짜 분석을 내놓는 실제 코봇 캡처본.
* **[화면 2 - 원인 증명 (일반 LLM)]:** 최신 LLM(Claude 등)에 원시 API만 주었을 때도 코봇과 똑같이 엉터리 분석을 내놓는 캡처본. (앱의 문제가 아니라 데이터 구조의 문제임을 증명)
* **[화면 3 - 해결책 증명 (프록시 LLM + 팩트체크 JSON)]:** 동일한 LLM에 우리가 추출한 팩트체크 JSON을 함께 주입했을 때, *"2019년 기준 변경으로 비재생폐기물이 제외되었으므로 단순 수평 비교는 불가능합니다"*라고 완벽하게 오독을 방어하는 시연 캡처본.

---

### 5. 확장 가능성을 제시하는 정책 제안

**[제안명: TDO-KR 시스템 고도화를 위한 '레드티밍 기반 통계 메타데이터 자동 추출 파이프라인' 도입]**

본 프로젝트는 국가데이터처가 당면한 2026-2028년 1단계 기반 구축 사업의 일정과 예산을 획기적으로 혁신할 수 있는 실제적인 프로토타입입니다.

* **환각의 구조적 원인 해결 및 챗봇 신뢰성 회복:** 현재 서비스 중인 코봇의 환각은 모델의 지능 부족이 아닌 '통계 맥락(메타데이터)의 부재'에서 기인함을 3단 프록시 테스트로 실증했습니다. 본 파이프라인이 생성한 맥락 규칙을 미들웨어로 장착하면, 대국민 챗봇의 가짜뉴스 생성 위험을 발생 전 단계에서 선제적으로 차단할 수 있습니다.
* **수작업에서 검수 작업으로의 행정 혁신:** 공무원 조직이 수만 개의 통계 규칙을 백지상태에서 타이핑하던 구조에서, **AI가 단 몇 초 만에 추출해 준 핵심 함정(JSON 스키마) 초안을 눈으로 확인하고 '컨펌'만 하는 구조**로 행정 프로세스가 바뀝니다. 이는 데이터 구축 소요 시간을 1/100로 단축하며 막대한 예산을 절감합니다.
* **미래 국가 통계 AI(MODI)를 위한 고품질 자산화:** 인간의 최종 검수를 거쳐 본 파이프라인으로 누적될 구조화된 백과사전(JSON) 데이터는, 향후 2027년 이후 도입될 통계 특화 거대언어모델(MODI)의 답변 신뢰성을 보장하는 **가장 완벽한 검색증강생성(RAG)용 고품질 지식 데이터셋**으로 확장될 것입니다.
